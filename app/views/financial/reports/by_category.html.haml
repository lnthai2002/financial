.container
  =form_for :category, :url=>for_category_reports_path, :method=>:get do |f|
    -@categories.each do |e|
      .form-group
        =f.check_box e.description
        =f.label e.description, e.description
    .form-group
      =f.submit 'generate'