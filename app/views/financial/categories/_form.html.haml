= form_for(@category) do |f|
  = f.error_messages
  %fieldset
    %legend Category
    %table
      %tr
        %th=f.label "Description"
        %td=f.text_field :description, :placeholder=>"Short description"
      %tr
        %th=f.submit "OK"